server:
  port: 1201
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/excarlibur/config
          search-paths: '{application}'
#          username:
#          password:


